server:
  port: 9090
spring:
  h2:
    console:
      enabled: true

  application:
    name: BOOK-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    hostname: localhost